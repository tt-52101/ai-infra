services:
  vllm-verify:
    image: ${VLLM_IMAGE:-vllm/vllm-openai:latest}
    container_name: vllm-lmcache-verify
    network_mode: host
    environment:
      - CUDA_VISIBLE_DEVICES=${VERIFY_GPU_ID:-0}
      - NCCL_P2P_DISABLE=${NCCL_P2P_DISABLE:-1}
      - NCCL_IB_DISABLE=${NCCL_IB_DISABLE:-1}
      - NCCL_SHM_DISABLE=${NCCL_SHM_DISABLE:-0}
      - NCCL_DEBUG=${NCCL_DEBUG:-WARN}
      - NCCL_CUMEM_ENABLE=${NCCL_CUMEM_ENABLE:-0}
      - NCCL_CUMEM_HOST_ENABLE=${NCCL_CUMEM_HOST_ENABLE:-0}
      - VLLM_WORKER_MULTIPROC_METHOD=${VLLM_WORKER_MULTIPROC_METHOD:-spawn}
      - OMP_NUM_THREADS=${OMP_NUM_THREADS:-1}
      - LMCACHE_CONFIG_FILE=/vllm-workspace/lmcache_config.yaml
    volumes:
      - ${MODEL_PATH:-/data/temp/yhb/Qwen3.5-35B-A3B}:/model:ro
      - ./lmcache_config.yaml:/vllm-workspace/lmcache_config.yaml:ro
    ipc: host
    ulimits:
      memlock: -1
      nofile:
        soft: 65536
        hard: 65536
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              device_ids: ['${VERIFY_GPU_ID:-0}']
              capabilities: [gpu]
    command: >
      /model
      --quantization ${MODEL_QUANTIZATION:-awq}
      --dtype ${MODEL_DTYPE:-float16}
      --tensor-parallel-size 1
      --disable-custom-all-reduce
      --trust-remote-code
      --gpu-memory-utilization ${GPU_MEMORY_UTILIZATION:-0.80}
      --max-model-len ${MAX_MODEL_LEN:-32768}
      --max-num-seqs ${VERIFY_MAX_NUM_SEQS:-8}
      --enable-prefix-caching
      --api-key ${VLLM_API_KEY:-sk-mvp-change-me}
      --kv-transfer-config '{"kv_connector":"LMCacheMPConnector","kv_connector_module_path":"lmcache.integration.vllm.lmcache_mp_connector","kv_role":"kv_producer","kv_connector_extra_config":{"lmcache.mp.port":${LMCACHE_MP_PORT:-6555}}}'
      --host 127.0.0.1
      --port 8003
    depends_on:
      lmcache-server:
        condition: service_healthy
    healthcheck:
      test:
        - CMD-SHELL
        - python -c "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8003/health', timeout=5).read()"
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 300s
    stop_grace_period: 120s
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: "50m"
        max-file: "5"