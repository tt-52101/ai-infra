version: '3.8'

# Base Compose file.
# Do not add services here; keep deployable capabilities in:
# - docker-compose.lmcache.yml
# - docker-compose.prefill.yml
# - docker-compose.decode.yml
# - docker-compose.gateway.yml
#
# Use ../ops/pd-stack.sh or ../ops/pd-stack.ps1 to combine files safely.

networks:
  ds-pd-network:
    driver: bridge
